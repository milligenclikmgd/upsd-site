document.addEventListener("DOMContentLoaded",()=>{const h=document.querySelector(".header"),hamb=h?.querySelector(".hamb"),nav=h?.querySelector(".nav");if(hamb&&nav)hamb.addEventListener("click",()=>nav.classList.toggle("open"));h?.querySelectorAll("[data-page]").forEach(b=>b.addEventListener("click",()=>{location.href=b.dataset.page}));h?.querySelectorAll(".mega").forEach(mega=>{const cols=[...mega.querySelectorAll(".mega-column")],titles=cols.flatMap(col=>[...col.querySelectorAll(".mega-title")]),linkLists=cols.map(col=>[...col.querySelectorAll("a")]);let sequence=[...titles];for(let row=0;row<Math.max(0,...linkLists.map(list=>list.length));row++)linkLists.forEach(list=>{if(list[row])sequence.push(list[row])});sequence.forEach((el,index)=>el.style.setProperty("--reveal-index",String(index)));});});